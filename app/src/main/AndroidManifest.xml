<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="carlosperez.pmdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Actividad Principal -->
        <activity android:name="carlosperez.pmdm.MainActivity">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <!-- Actividad Principal -->

        <activity android:name="carlosperez.pmdm.RegistroActivity" />
        <activity android:name="carlosperez.pmdm.ApuestasActivity" />
        <activity android:name="carlosperez.pmdm.AjustesActivity" />
        <activity android:name="carlosperez.pmdm.AyudaActivity" />
        <activity android:name="carlosperez.pmdm.InfoActivity" />

        <!-- Activity PreferenciasFragment -->
        <activity android:name="carlosperez.pmdm.PreferenciasFragment" />
        <activity android:name="carlosperez.pmdm.SetPreferenciasActivity" />

    </application>

</manifest>